# VK Utils - Консольный инструмент для работы с VK API

Консольное приложение на Laravel для автоматизации работы с VKontakte API. Инструмент предоставляет набор команд для анализа постов, комментариев, поиска контента и мониторинга активности в группах VK.

## 🚀 Возможности

- **Поиск контента для републикации** - автоматический поиск постов без репостов
- **Поиск дублей** - нахождение повторяющихся постов по текстовому паттерну
- **Анализ комментариев** - поиск комментариев конкретного автора во всех постах группы
- **Мониторинг групп** - проверка последних постов в списке групп с метриками (лайки, репосты)
- **Работа с постами** - получение постов, комментариев, закрепление постов
- **Работа с аудио** - добавление аудио в группы

## 📋 Требования

- PHP 8.1+ (рекомендуется PHP 8.2+)
- Composer
- Токен доступа VK API

## 🔧 Установка

### Быстрая установка (с Makefile)

Если у вас установлен `make`, используйте команду для полной настройки:

```bash
make setup
```

Эта команда автоматически выполнит:
- Установку зависимостей Composer
- Генерацию ключа приложения
- Запуск миграций базы данных

### Ручная установка

1. Клонируйте репозиторий или скачайте проект
2. Установите зависимости:
```bash
composer install
# или
make install
```

3. Скопируйте файл `.env.example` в `.env` (если его нет):
```bash
cp .env.example .env
```

4. Сгенерируйте ключ приложения:
```bash
php artisan key:generate
# или
make key-generate
```

5. Запустите миграции:
```bash
php artisan migrate
# или
make migrate
```

6. Настройте переменные окружения в `.env`:
```env
VK_TOKEN=ваш_токен_vk_api
VK_API_VERSION=5.122
VK_VERIFY_SSL=false
VK_ACCOUNT_BASE_URL=https://vk.com
```

## ⚙️ Настройка

### Получение токена VK API

1. Перейдите на [VK Developers](https://vk.com/dev)
2. Создайте приложение
3. Получите токен доступа с необходимыми правами:
   - `wall` - для работы со стенами
   - `groups` - для работы с группами
   - `audio` - для работы с аудио (если нужно)

### Подготовка списка групп

Для команд, работающих со списком групп, создайте файл `storage/app/resources.csv` со списком URL групп:

```csv
https://vk.com/groupname1
https://vk.com/groupname2
https://vk.com/anothergroup
```

Каждая строка должна содержать полный URL группы VK.

## 📖 Команды

### `vk:check` - Проверка последних постов в группах

Проверяет последние посты во всех группах из списка `resources.csv` и выводит таблицу с метриками.

**Использование:**
```bash
php artisan vk:check
php artisan vk:check --cached
php artisan vk:check --delay=1
```

**Параметры:**
- `--cached` - использовать кеш из предыдущего запуска (не делать запросы к API)
- `--delay=0.3` - задержка между запросами в секундах (по умолчанию 0.3)

**Пример вывода:**
```
+------------------------------------------+------------------+----------+-------+---------+
| Post                                     | Group name       | Group ID | Likes | Reposts |
+------------------------------------------+------------------+----------+-------+---------+
| Привет! Это первый пост в группе...      | Моя группа       | 12345678 | 15    | 3       |
| Новости дня: важная информация...        | Другая группа    | 87654321 | 42    | 8       |
+------------------------------------------+------------------+----------+-------+---------+
```

**Примечание:** Результаты кешируются в `storage/app/cache-resources.json` для быстрого повторного просмотра.

---

### `vk:candidate` - Поиск кандидатов для републикации

Ищет посты со стены указанной группы/пользователя, которые можно использовать для републикации.

**Использование:**
```bash
php artisan vk:candidate --owner=-12345678
php artisan vk:candidate --owner=-12345678 --noreposts
```

**Параметры:**
- `--owner` - ID владельца стены (обязательный). Для групп используйте отрицательное число (например, `-12345678`)
- `--noreposts` - исключить посты, которые уже были отрепощены

**Пример:**
```bash
php artisan vk:candidate --owner=-12345678 --noreposts
```

**Вывод:**
```
Текст поста, который можно использовать для републикации
https://vk.com?w=wall-12345678_12345
LIKES 42 REPOSTS 0
====================================================================================================
```

**Примечание:** Команда проходит по всем постам стены, поэтому может занять время. Между постами есть задержка 5 секунд.

---

### `vk:dups` - Поиск дублей постов

Ищет посты, содержащие указанный текст (поиск по регулярному выражению).

**Использование:**
```bash
php artisan vk:dups --owner=-12345678 --text="текст для поиска"
```

**Параметры:**
- `--owner` - ID владельца стены (обязательный)
- `--text` - текстовый паттерн для поиска (обязательный). Поддерживает регулярные выражения

**Примеры:**
```bash
# Простой поиск
php artisan vk:dups --owner=-12345678 --text="важная новость"

# Поиск с регулярным выражением (case-insensitive)
php artisan vk:dups --owner=-12345678 --text="новость|объявление"
```

**Вывод (JSON):**
```json
{"url":"https://vk.com?w=wall-12345678_12345","likes":10,"reposts":2}
{"url":"https://vk.com?w=wall-12345678_12346","likes":5,"reposts":0}
```

---

### `post:comments` - Получение комментариев к посту

Получает все комментарии к указанному посту.

**Использование:**
```bash
php artisan post:comments --owner=-12345678 --post=12345
```

**Параметры:**
- `--owner` - ID владельца стены (обязательный)
- `--post` - ID поста (обязательный)

**Пример:**
```bash
php artisan post:comments --owner=-12345678 --post=12345
```

**Вывод:** Полная информация о каждом комментарии (структура объекта комментария VK API)

---

### `comments:find` - Поиск комментариев автора

Ищет все комментарии указанного пользователя во всех постах группы.

**Использование:**
```bash
php artisan comments:find --owner=-12345678 --author=98765432
```

**Параметры:**
- `--owner` - ID владельца стены (группы) (обязательный)
- `--author` - ID автора комментариев (обязательный)

**Пример:**
```bash
php artisan comments:find --owner=-12345678 --author=98765432
```

**Вывод:**
```
"Текст комментария пользователя"
https://vk.com/wall-12345678_12345?reply=67890
```

**Примечание:** Команда проходит по всем постам группы, что может занять значительное время для больших групп.

---

### `vk:groups:info` - Информация о группах

Получает и выводит информацию о всех группах из списка `resources.csv`. Команда резолвит короткие имена групп в ID и получает полную информацию о каждой группе. Полезно для проверки корректности списка групп и получения их ID.

**Использование:**
```bash
php artisan vk:groups:info
```

**Вывод:**
```
+------------------+----------+------+
| Name             | ID       | Type |
+------------------+----------+------+
| Моя группа       | 12345678 | group|
| Другая группа    | 87654321 | group|
+------------------+----------+------+
```

**Примечание:** Команда использует progress bar для отображения прогресса обработки групп. Задержка между запросами: 0.5 секунды.

## 🔍 Как узнать ID группы или пользователя

### ID группы:
1. Откройте группу в VK
2. Посмотрите на URL: `https://vk.com/club12345678` или `https://vk.com/groupname`
3. Если видите `club` или число в URL - это ID группы
4. Для использования в командах добавьте минус: `-12345678`

### ID пользователя:
1. Откройте профиль пользователя
2. В URL будет `https://vk.com/id12345678` или `https://vk.com/username`
3. Число после `id` - это ID пользователя

### Резолв короткого имени:
Можно использовать короткое имя группы (screen name) вместо ID. Инструмент автоматически резолвит его в ID.

## 📁 Структура файлов

```
storage/app/
├── resources.csv          # Список групп (создается вручную)
└── cache-resources.json   # Кеш результатов vk:check (создается автоматически)
```

## ⚠️ Ограничения VK API

VK API имеет ограничения на количество запросов:
- **Rate limiting:** Инструмент автоматически добавляет задержки между запросами
- **Лимиты:** При превышении лимитов API вернет ошибку - нужно подождать

Рекомендуется:
- Использовать задержки между запросами (параметр `--delay`)
- Не запускать несколько команд одновременно
- Использовать кеш (`--cached`) для повторного просмотра результатов

## 🐛 Решение проблем

### Ошибка "Invalid token"
- Проверьте правильность токена в `.env`
- Убедитесь, что токен не истек
- Проверьте права доступа токена

### Ошибка "Access denied"
- Убедитесь, что токен имеет необходимые права (`wall`, `groups`, `audio`)
- Проверьте, что группа/пользователь доступны для токена

### Ошибка "Too many requests"
- Увеличьте задержку между запросами (`--delay`)
- Подождите некоторое время перед повторным запуском

### Файл `resources.csv` не найден
- Создайте файл `storage/app/resources.csv`
- Добавьте в него URL групп, по одной на строку

## 📝 Примеры использования

### Ежедневная проверка активности в группах
```bash
# Первый запуск - сбор данных
php artisan vk:check --delay=0.5

# Повторный просмотр без запросов к API
php artisan vk:check --cached
```

### Поиск контента для публикации
```bash
# Найти оригинальные посты без репостов
php artisan vk:candidate --owner=-12345678 --noreposts
```

### Проверка на дубли
```bash
# Найти все посты с определенным текстом
php artisan vk:dups --owner=-12345678 --text="важная новость"
```

### Анализ активности пользователя
```bash
# Найти все комментарии пользователя в группе
php artisan comments:find --owner=-12345678 --author=98765432
```

## 📄 Лицензия

MIT License

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте настройки в `.env`
2. Убедитесь, что токен VK API валиден
3. Проверьте логи Laravel в `storage/logs/`

## 🛠️ Makefile команды

Проект включает Makefile для упрощения работы с основными командами. Просмотреть все доступные команды:

```bash
make help
```

### Основные команды

```bash
# Полная настройка проекта (установка + ключ + миграции)
make setup

# Установка зависимостей
make install

# Обновление зависимостей
make update

# Запуск миграций
make migrate

# Очистка кешей
make clean

# Запуск тестов
make test
```

### Команды для работы с VK API

```bash
# Проверить последние посты в группах
make vk-check

# Получить информацию о группах
make vk-groups-info
```

---

**Примечание:** Этот инструмент создан для личного использования и автоматизации работы с VK API. Соблюдайте правила использования VK API и не злоупотребляйте запросами.
